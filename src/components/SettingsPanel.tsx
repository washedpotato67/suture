import { KeyRound, Shield } from "lucide-react";
import type { OrgRole } from "../lib/data";
export function SettingsPanel({ role }: { role: OrgRole | null }) { return <section className="two-column"><article className="panel"><div className="panel-heading compact"><div><h2>Workspace access</h2><p>Role assignments are enforced by Supabase RLS and server-side RPCs.</p></div><Shield size={18} /></div><dl className="detail-list"><div><dt>Your role</dt><dd>{role ?? "demo operator"}</dd></div><div><dt>Tenant boundary</dt><dd>organization membership</dd></div></dl></article><article className="panel"><div className="panel-heading compact"><div><h2>Provider secrets</h2><p>Cleanverse keys stay in the local server environment. They are never displayed in this workspace.</p></div><KeyRound size={18} /></div><p className="muted-copy">Update local server configuration outside the browser, then retry the integration diagnostic.</p></article></section>; }
