# Limitations and external dependencies

1. Monad RPC, deployed addresses, indexer checkpoint, and contract verification are not configured.
2. Resolved on 2026-08-09. Hosted Supabase project `jsdlpkdhenaniejevqdp` (Central EU, Frankfurt) has all nine migrations applied; `suture-orchestrator` and `cleanverse-webhook` are deployed and ACTIVE; the console is deployed at https://suture-one.vercel.app and renders against hosted Supabase. Two consequences. (a) The Edge Function CORS headers were confirmed necessary by experiment: a probe function returning no CORS headers received none from the hosted platform, so without the explicit `Access-Control-Allow-Origin` every browser call to `suture-orchestrator` would be blocked in production. Local Kong injects the header and masks this. (b) Hosted Edge Functions egress from Supabase infrastructure, not the developer machine, so Cleanverse provider calls from the hosted orchestrator will fail the provider IP allowlist. The provider evidence in item 5 is reproducible locally only until a stable egress address is allowlisted.
3. Resolved on 2026-08-09. Local Edge Function calls failed with `TypeError: Invalid URL: 'http://supabase_edge_runtime_suture-v0.1:8081/…'`. The cause was `project_id`, which the CLI defaults to the working-directory name: the id becomes the container hostname, and a host whose final label is numeric is parsed as an IPv4 address by the WHATWG URL parser, so `…-v0.1` failed to parse before any function was reached. `project_id` is now `suture-v0-1`. A `POST` to `suture-orchestrator` with a valid anon JWT returns the function's own `{"error":"authentication required"}` and its own CORS headers, confirming the function executes. The function's `OPTIONS` handler is still unproven locally because Kong answers preflight itself; that path matters only for a hosted deployment. An authenticated end-to-end preflight decision record remains unproven and depends on item 5.
4. Cleanverse documentation exposes no generic asset-verification endpoint and no credential revocation or policy-change webhook. SUTURE blocks unsupported or unavailable risk evaluation instead of inferring success.
5. Substantially closed on 2026-08-09. A live sandbox preflight produced a `BLOCK` decision on `VERIFIED` evidence: `query_apass` → `0000` with an active A-Pass, `validator/verify` → `0000` with `valid: false`, persisted with provider request ID `542ff062-2f5b-4e2c-95b3-0a9343e6b43e` and a SHA-256 response digest. The api-id is authorized and the egress IP is allowlisted. Four caveats remain. (a) The A-Token scope path is unverified: the only unpaused sandbox A-Token returns `ComplianceFailed` for every wallet tested, including wallets with active A-Passes, so `verify_apass` was never exercised successfully. (b) The API key itself is untested, because documented read endpoints transmit only `api-id`; the key is used for AES envelopes and webhook HMAC. (c) The webhook receiver was verified locally on 2026-08-09 against a payload shaped like the docs' UAT-verified `ATOKEN_APPLY_RESULT` example and signed with the real sandbox api-key: valid signature accepted (204), tampered signature rejected (401), wrong event type and malformed delivery id rejected (400), bad envelope types rejected (400), and three accepted POSTs produced exactly one stored row, confirming the delivery-id, request-id, and payload-digest idempotency guards. No provider-originated delivery has been received, which still requires a public callback URL and an A-Token apply flow. Note that the webhook must be deployed with JWT verification disabled (`supabase functions deploy cleanverse-webhook --no-verify-jwt`), because Cleanverse sends no JWT. (d) The sandbox API key was exposed in cleartext in an assistant session log on 2026-08-09 and was not rotated, so the decision record above is reproducible but is not independently trustworthy provenance; rotate the key and re-run before citing it as evidence. The source asset evidence label was also set manually for that run and is not provider-attested.
6. The current remediation database flow is simulated. A production executor needs verified chain submission, uncertain-state reconciliation, and independently retained transaction receipts.
7. Contract source passed Foundry tests but is not audited, deployed, or formally verified.
